# Build, lint, package, and test commands require no environment variables.

# Optional only when manually invoking the Slack reference plugin.
# Prefer Flyto2 Core secret resolution and never commit a real token.
SLACK_BOT_TOKEN=
