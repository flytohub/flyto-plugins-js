# Reference Plugins

- `form-builder`: dynamic forms and approval dialogs.
- `image-crop`: interactive crop and image export.
- `slack`: message creation and public-channel discovery.

Each plugin contains package metadata, `plugin.yaml`, real handler tests, and a
package README. The [generated contracts](../docs/generated/plugin-contracts.md)
are derived from the manifests.
